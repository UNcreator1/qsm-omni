# Karpathy LLM Wiki

This is the seed common-truth layer for Quantum Swarm V3.

Runtime runs can regenerate this file from verified lake artifacts with:

```bash
qsm wiki -root .
```

Do not commit generated local lake dumps with absolute workstation paths. Durable memory lives in `.lake/`, shared cache records, and promoted wiki artifacts produced during a run.
